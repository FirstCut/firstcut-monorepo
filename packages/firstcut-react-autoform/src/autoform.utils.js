
//TODO this needs to be generated
export function removeNonDomFields(props) {
  let {regEx, enumOptions, single_file, customType, custom, serviceFilter, restricted, record, ...dom_props} = props;
  return dom_props;
}
