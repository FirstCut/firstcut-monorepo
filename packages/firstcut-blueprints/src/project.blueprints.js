
export const customer_testimonial = Object.freeze({
  label: "Customer Testimonial",
  defaults: {},
  default_children: {
    "SHOOT": {blueprint: 'CORPORATE_INTERVIEWS'}
    }
});

export const not_a_template = Object.freeze({
  label: "Not a Template",
  defaults: {},
  default_children: {
    "SHOOT": {blueprint: 'CORPORATE_INTERVIEWS'}
    }
});
