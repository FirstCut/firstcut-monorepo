
// TODO this needs to be generated
export function removeNonDomFields(props) {
  const {
    allowedValues,
    locationTypes,
    serviceDependency,
    helpText,
    regEx,
    optional,
    enumOptions,
    singleFile,
    customType,
    custom,
    serviceFilter,
    restricted,
    record,
    sortBy,
    unique,
    ...domProps
  } = props;
  return domProps;
}
